import Box from '@mui/material/Box/Box';
import { EstateProperties } from '..';

function SideEstatePanel(props: { estate: EstateProperties }) {
    return (
        <Box width="100%" sx={{ backgroundColor: '#dddddd' }}>
            SIDE PANEL (WIP)
        </Box>
    );
}

export default SideEstatePanel;
